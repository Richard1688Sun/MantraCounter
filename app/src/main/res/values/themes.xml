<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MantraCounter" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gold_light</item>
        <item name="colorPrimaryVariant">@color/gold_dark</item>
        <item name="colorOnPrimary">@color/gold_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/button_green</item>
        <item name="colorSecondaryVariant">@color/button_green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="basicBackground" parent="Theme.AppCompat.DayNight">
        <item name="android:background">@color/gold_light</item>
    </style>

    <style name="HiddenTitleTheme" parent="Theme.MantraCounter">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="noRipple" parent="Theme.AppCompat.DayNight">
        <item name="rippleColor">@android:color/transparent</item>
        <item name="backgroundColor">@color/gold_light</item>
        <item name="backgroundTint">@color/gold_light</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>